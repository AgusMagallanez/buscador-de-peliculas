import React from "react";
import ReactDOM from "react-dom/client";
import { AppMovies } from "./AppMovies.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AppMovies />
  </React.StrictMode>
);
